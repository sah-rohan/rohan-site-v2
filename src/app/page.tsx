import PixelDesk from "@/components/PixelDesk";

export default function Home() {
  return (
    <main className="fixed inset-0 w-screen h-screen bg-[#0a0a0a] overflow-hidden">
      <PixelDesk />
    </main>
  );
}
